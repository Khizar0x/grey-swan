# Dedicated devnet RPC for the Anchor CLI (anchor test / anchor deploy).
# Export this before running anchor commands to avoid public-endpoint rate
# limits, e.g.: export $(cat .env | xargs) && anchor test
ANCHOR_PROVIDER_URL=
